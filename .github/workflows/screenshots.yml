name: Screenshots

# Manual, opt-in job that captures real app screenshots for PR review.
# Runs the ScreenshotTests UI test (which boots the app in demo mode and
# grabs each tab), then exports the screenshots from the .xcresult and
# uploads them as a downloadable `app-screenshots` artifact.
#
# workflow_dispatch only — never gates a PR, so it can't block merges.
#
# Uses the `PeptideUICapture` scheme, which builds the app and the UI tests
# and nothing else. The main `Peptide` scheme's test action also compiles
# `PeptideTests`, which currently doesn't build — that would gate the one job
# whose purpose is to let a human look at the app behind an unrelated backlog.
#
# Captures three passes per tab: dark, light, and Accessibility XXXL. Light
# and XXXL exist because the trait-resolving colour tokens and the six-step
# type scale are both invisible to a single default-appearance run.
on:
  workflow_dispatch:
    inputs:
      simulator:
        description: "Simulator device to capture on (blank = first available iPhone)"
        required: false
        default: ""
        type: string
  # TEMPORARY. Dispatching this workflow needs an `actions: write` token,
  # which the agent working this branch does not have — so the one job whose
  # entire purpose is to let a human look at the app could never be started
  # by the thing that needed it. A push trigger scoped to this one branch
  # needs no special permission.
  #
  # Delete this `push:` block once the branch merges. It costs a macOS
  # runner (~15 min) on every push while it is here.
  push:
    branches:
      - claude/app-review-design-polish-s14ish

permissions:
  contents: read

jobs:
  screenshots:
    runs-on: macos-15
    timeout-minutes: 30
    steps:
      - uses: actions/checkout@v4

      - uses: maxim-lobanov/setup-xcode@v1
        with:
          xcode-version: latest-stable

      - name: Install XcodeGen
        run: brew install xcodegen || brew reinstall xcodegen

      - name: Generate Xcode project
        run: xcodegen generate

      - name: Pick a simulator
        # `inputs.simulator` is empty on a push event, which would have made
        # the destination `name=,OS=latest`. And this workflow has never
        # actually run, so its old hardcoded "iPhone 16" was never verified
        # to exist on the runner's Xcode. Resolve a real device instead.
        id: sim
        run: |
          WANT="${{ inputs.simulator }}"
          if [ -n "$WANT" ] && xcrun simctl list devices available | grep -q "$WANT ("; then
            echo "name=$WANT" >> "$GITHUB_OUTPUT"
          else
            FOUND=$(xcrun simctl list devices available \
              | grep -oE '^\s+iPhone [^(]+' | sed 's/^ *//;s/ *$//' | tail -1)
            if [ -z "$FOUND" ]; then
              echo "::error::No iPhone simulator available on this runner."
              xcrun simctl list devices available
              exit 1
            fi
            echo "Using $FOUND"
            echo "name=$FOUND" >> "$GITHUB_OUTPUT"
          fi

      - name: Run screenshot UI test
        run: |
          xcodebuild test \
            -project Peptide.xcodeproj \
            -scheme PeptideUICapture \
            -destination "platform=iOS Simulator,name=${{ steps.sim.outputs.name }},OS=latest" \
            -configuration Debug \
            CODE_SIGN_IDENTITY="" \
            CODE_SIGNING_REQUIRED=NO \
            CODE_SIGNING_ALLOWED=NO \
            -only-testing:PeptideUITests/ScreenshotTests \
            -resultBundlePath Screenshots.xcresult

      - name: Export screenshots from xcresult
        if: always()
        run: |
          set -x
          mkdir -p screenshots
          # Export every attachment (the captures are .keepAlways) plus a
          # manifest mapping the exported files back to their names.
          xcrun xcresulttool export attachments \
            --path Screenshots.xcresult \
            --output-path screenshots || true
          ls -laR screenshots || true

      - name: Upload screenshots
        if: always()
        uses: actions/upload-artifact@v4
        with:
          name: app-screenshots
          path: screenshots/**
          if-no-files-found: warn
          retention-days: 14
