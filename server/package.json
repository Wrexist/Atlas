{
  "name": "peptidex-meal-scan-proxy",
  "version": "1.0.0",
  "private": true,
  "description": "Vercel serverless proxy that holds the Anthropic API key for the Atlas iOS meal scanner.",
  "type": "module",
  "scripts": {
    "test": "node --test"
  },
  "engines": {
    "node": ">=18"
  }
}
