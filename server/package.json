{
  "name": "peptidex-meal-scan-proxy",
  "version": "1.0.0",
  "private": true,
  "description": "Vercel serverless proxy that holds the Anthropic API key for the PeptideX iOS meal scanner.",
  "type": "module",
  "engines": {
    "node": ">=18"
  }
}
